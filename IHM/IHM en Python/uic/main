from PyQt6 import QtCore, QtGui, QtWidgets
from 1_choix_doc_user.py import *


def main():
    #Création de l'application, en passant en argumant argv (vecteur contenant dans les arguments en ligne de commande)
    app = QtWidgets.QApplication(sys.argv)
    #Création de la fenêtre
    widget = QtWidgets.QWidget()
    #Création de notre widget custom, en le parentant à la fenêtre
    mycustomwidget = Ui_Form()
    mycustomwidget.setupUi(widget)
    #On montre la fenêtre
    widget.show()
    #On execute la mainloop
    app.exec()

if __name__=="__main__":
    main()


def verification(self.validation.clicked()):
    if self.validation.clicked['QAbstractButton*'].connect(self.NomPrenomEdit_2.copy) =! "Louis-Ferdinand Dartencet":
        self.label.set
        if self.validation.clicked['QAbstractButton*'].connect(self.MdpEdit_2.copy) == "25112000":